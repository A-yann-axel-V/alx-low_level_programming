#include <stdio.h>

/**
 * main - Prints the number of arguments passed
 * @argc: arguments count
 * @argv: arguments
 *
 * Return: 0
 */

int main(int argc, char *argv)
{
	printf("%d", argc);

	return (0);
}
